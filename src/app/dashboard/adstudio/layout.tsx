export const metadata = {
	title: 'Ad Studio | Dashboard',
	description: 'Create and manage your advertising workflows',
};

export default function AdStudioLayout({
	children,
}: {
	children: React.ReactNode;
}) {
	return <>{children}</>;
}
